let mongoose;_526‍.x([["default",()=>_526‍.o]]);_526‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);

const { Schema } = mongoose;
const { ObjectId } = mongoose.Schema;

const hotelSchema = new Schema(
   {
      title: {
         type: String,
         required: 'Title is requied',
      },
      content: {
         type: String,
         required: 'Content is requied',
         maxlength: 1000,
      },
      location: {
         type: String,
      },
      price: {
         type: Number,
         required: 'Price is requied',
         trim: true,
      },
      postedBy: {
         type: ObjectId,
         ref: 'User',
      },
      image: {
         data: Buffer,
         contentType: 'String',
      },
      from: {
         type: Date,
      },
      to: {
         type: Date,
      },
      bed: {
         type: Number,
      },
   },
   { timestamps: true }
);

_526‍.d(mongoose.model('Hotel', hotelSchema));
