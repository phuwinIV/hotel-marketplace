let express,readdirSync,cors,mongoose;_114‍.w("express",[["default",["express"],function(v){express=v}]]);_114‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);_114‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_114‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);



const morgan = require('morgan');

require('dotenv').config();

const app = express();

// db connection
mongoose
   .connect(process.env.DATABASE, {
      useNewUrlParser: true,
      useUnifiedTopology: true,
   })
   .then(() => console.log('DB conected'))
   .catch((err) => _114‍.g.console.log('DB ERROR', err));

// middlewareds
app.use(cors());
app.use(morgan('dev'));
app.use(express.json());
// route middleware
readdirSync('./routes').map((r) => app.use('/api', require(`./routes/${r}`)));

const port = process.env.PORT || 5000;
app.listen(port, () => console.log(`Server is runing in port ${port}`));
